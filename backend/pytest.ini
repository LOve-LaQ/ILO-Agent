[pytest]
# 只收集本目录下的 tests/，避免误扫 ilo/Lib/site-packages 里的第三方测试
testpaths = tests
# eval 门禁默认不执行（会真实调用 LLM 花钱）；显式 `pytest -m eval` 时命令行参数覆盖 addopts
addopts = -m "not eval" --strict-markers
markers =
    eval: LLM-as-judge 输出质量评测门禁（默认 deselect，仅 `pytest -m eval` 执行）
